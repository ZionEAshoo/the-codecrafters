## The Summary of GO Comments:

A comment in GO is a text-line that is ignored during execution.
We have single-line comments (where the text starts with //) and multi-line comments (where the text starts with /* and ends with */)