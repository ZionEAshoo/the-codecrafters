## The Summary of GO Constants:

The 'const' keyword declares the variable as "constant"

Constant names are usually written in uppercase letters.

Constants can be declared both inside and outside of a function.

We have two types of constants which are typed constants and untyped constants.

